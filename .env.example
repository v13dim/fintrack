# FinTrack – environment variables template
# Copy to .env and fill with your values. Do not commit .env.
# See docs/guides/certificate-management-guide.md for signing and CI secrets.

# -----------------------------------------------------------------------------
# Android release signing (optional; only if using release keystore locally)
# For CI, use GitHub Secrets instead.
# -----------------------------------------------------------------------------
# ANDROID_KEYSTORE_PATH=android/app/fintrack-release.keystore
# ANDROID_KEYSTORE_PASSWORD=
# ANDROID_KEY_ALIAS=fintrack
# ANDROID_KEY_PASSWORD=

# -----------------------------------------------------------------------------
# Firebase App Distribution (for Fastlane / CI; use secrets in CI)
# -----------------------------------------------------------------------------
# FIREBASE_CI_TOKEN=
# FIREBASE_APP_ID_ANDROID=
